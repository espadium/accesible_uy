.row
  .column
    %p= flash[:notice]

.row
  .column
    = form_for @subscription do |f|
      %p Ingresa tu e-mail para recibir notificaciones de los lugares y eventos accesibles:

      = f.label :email
      = f.email_field :subscriptor
      %p.antispam
        Leave this empty:
        %input{ name: 'url' }

      %p Puedes elegir entre algunos de estos temas de interés o dejar vacío si quieres recibir toda la información:

      %ul
        - @accessibilities.each do |a|
          %li
            = f.label a.name.to_sym
            = check_box_tag "accessibilities[#{ a.id }]"

      = f.submit 'Ingresar'
